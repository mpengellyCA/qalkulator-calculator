// Embeds the application icon into qalkulator.exe so Windows shows it in
// Explorer, the taskbar and the title bar (compiled by windres on MinGW).
// IDI_ICON1 (lowest id) is what Windows uses as the executable's icon.
IDI_ICON1 ICON "qalkulator.ico"
